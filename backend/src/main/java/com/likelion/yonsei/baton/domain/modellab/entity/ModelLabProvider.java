package com.likelion.yonsei.baton.domain.modellab.entity;

/** LLM provider for a ModelConfig / FineTuningJob. Only OPENAI is wired up today (spec section 2/17). */
public enum ModelLabProvider {
	OPENAI
}
